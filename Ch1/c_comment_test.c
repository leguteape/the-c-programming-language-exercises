/* This is a  comment * /
 * "/* This is a string
"
    * This;
is also;
a comment;
* /
    // "*/
"

    // This is also a C99 comment. Here the " string " is ignored";

    int
    main(void) {
    int a = 0;  // This is the return value
                /* char b = '//', c = '/*';  // Is this a comment?
                    "/* a = %d and b = '/*' */\n", a);
    printf("Is this legal? %c and %c\n", b, c);
    /* This returns a zero to the OS */ return a; /* This returns 0 to the OS */
}
